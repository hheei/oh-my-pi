<instruction>
Plan language: write the plan body in the same language as the user's request (the latest user message that triggered this plan). Keep the required English section headings — Context, Approach, Critical files & anchors, Verification, Assumptions & contingencies — as stable executor anchors. Paths, symbols, signatures, commands, and other literals stay verbatim. Interview questions via `ask` MUST use that same language. NEVER translate the user's request into English for the plan file.
</instruction>
